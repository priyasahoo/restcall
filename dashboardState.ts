import { ChartTypeState } from "../widgets/widgetState";

export class DashboardState {
  chartTypeChange: ChartTypeState[] = [];
  // add other properties related to dashboardState here
  // like
  // dataSource: DataSourceState[] = [];
}
